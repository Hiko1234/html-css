Command to install all packages:
npm install

The command to run the project:
npm run dev

The project is built automatically, but if necessary, the command to build the project:
npm run build

Command to deploy to gh-pages:
npm run deploy